<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Home - URL Shortener</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    html, body {
      height: 100%;
      font-family: 'Inter', sans-serif;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: #2d3748;
      overflow: hidden;
    }

    ::-webkit-scrollbar {
      display: none;
    }
    body {
      overflow: auto;
      -ms-overflow-style: none;
      scrollbar-width: none;
    }

    .floating-elements {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      pointer-events: none;
      overflow: hidden;
    }

    .floating-circle {
      position: absolute;
      border-radius: 50%;
      background: rgba(255, 255, 255, 0.1);
      animation: float 6s ease-in-out infinite;
    }

    .circle-1 {
      width: 80px;
      height: 80px;
      top: 20%;
      left: 10%;
      animation-delay: 0s;
    }

    .circle-2 {
      width: 120px;
      height: 120px;
      top: 60%;
      right: 15%;
      animation-delay: 2s;
    }

    .circle-3 {
      width: 60px;
      height: 60px;
      bottom: 30%;
      left: 20%;
      animation-delay: 4s;
    }

    @keyframes float {
      0%, 100% { transform: translateY(0px) rotate(0deg); }
      33% { transform: translateY(-20px) rotate(120deg); }
      66% { transform: translateY(10px) rotate(240deg); }
    }

    .header {
      text-align: center;
      color: white;
      padding: 1rem;
      position: relative;
      z-index: 2;
      border-radius: 10px;
      box-shadow: 5px 2px 10px rgba(43, 43, 43, 0.692);
    }

    .main-content {
      padding: 2rem;
      z-index: 2;
      position: relative;
      max-height: calc(100vh - 120px);
      overflow-y: auto;
      display: flex;
      flex-direction: column;
      align-items: center;
      
    }

    .form-section{
      max-width: 50vw;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      
      .one{
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
      }
      
    }
    
    .table-section {
      width: 100%;
      height:fit-content;
     
    }

    .form-section, .table-section {
      background: rgba(255, 255, 255, 0.95);
      backdrop-filter: blur(15px);
      border-radius: 20px;
      padding: 2rem;
      margin-bottom: 2rem;
      box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
      border: 1px solid rgba(255, 255, 255, 0.2);
    }

    .form-section label {
      font-weight: 600;
      font-size: 0.9rem;
      display: block;
      margin-bottom: 0.5rem;
    }

    .form-section input {
      width: 100%;
      padding: 0.875rem 1rem;
      border: 2px solid #e2e8f0;
      border-radius: 10px;
      margin-bottom: 1rem;
      font-size: 0.95rem;
    }

    .form-section button {
      padding: 0.875rem 1.5rem;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      border: none;
      border-radius: 10px;
      font-weight: 600;
      cursor: pointer;
      transition: transform 0.2s ease, box-shadow 0.2s ease;
    }

    .form-section button:hover {
      transform: translateY(-2px);
      box-shadow: 0 10px 25px rgba(102, 126, 234, 0.3);
    }

    .form-note {
      font-size: 0.85rem;
      color: #718096;
      margin-top: 0.5rem;
    }

    .short-url p {
      font-weight: 600;
      margin-top: 1rem;
    }

    .short-url a {
      color: #667eea;
      text-decoration: none;
    }

    .table-section h2 {
      margin-bottom: 1rem;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      background: white;
      border-radius: 10px;
      overflow: hidden;
    }

    th, td {
      padding: 0.75rem 1rem;
      text-align: left;
      border-bottom: 1px solid #e2e8f0;
    }

    th {
      background: #f7fafc;
      font-size: 0.9rem;
      color: #4a5568;
    }

    td a {
      color: #667eea;
      text-decoration: none;
    }

    .footer {
      text-align: center;
      color: rgba(255, 255, 255, 0.8);
      padding: 1rem;
      font-size: 0.8rem;
      z-index: 2;
      position: relative;
    }

    .logout-button {
      background: #e53e3e;
      color: white;
      padding: 0.75rem 1.25rem;
      border: none;
      border-radius: 10px;
      cursor: pointer;
      font-weight: 600;
      margin-top: 1rem;
      transition: all 0.3s ease;
    }

    .logout-button:hover {
      background: #c53030;
    }
  </style>
</head>
<body>
  <div class="floating-elements">
    <div class="floating-circle circle-1"></div>
    <div class="floating-circle circle-2"></div>
    <div class="floating-circle circle-3"></div>
  </div>

  <header class="header">
    <h1>URL Shortener</h1>
  </header>

  <main class="main-content">
    <section class="form-section">
      <form class="one" method="POST" action="/url">
        <label for="url">Enter the URL to make it short:</label>
        <input type="text" name="originalURL" id="url" placeholder="https://www.example.com" required />
        <button type="submit">Generate</button>
        <p class="form-note">(Make sure to input the full URL, e.g., https://www.example.com)</p>
      </form>

      <% if (locals.newURL) { %>
      <div class="short-url">
        <p>Short URL: <a href="<%= locals.newURL %>"><%= locals.newURL %></a></p>
      </div>
      <% } %>

      <form method="POST" action="/auth/logout">
        <button type="submit" class="logout-button">Logout</button>
      </form>
    </section>

    <% if (locals.user) { %>
    <section class="table-section">
      <h2>URL Statistics</h2>
      <table>
        <thead>
          <tr>
            <th>Sr. No.</th>
            <th>Original URL</th>
            <th>Short URL</th>
            <th>No. of Clicks</th>
          </tr>
        </thead>
        <tbody>
          <% locals.user.forEach((user, index) => { %>
          <tr>
            <td><%= index + 1 %></td>
            <td><%= user.originalURL %></td>
            <td>
              <a href="http://localhost:3000/url/<%= user.shortID %>">
                http://localhost:3000/url/<%= user.shortID %>
              </a>
            </td>
            <td><%= user.visited.length %></td>
          </tr>
          <% }) %>
        </tbody>
      </table>
    </section>
    <% } %>
  </main>

  <footer class="footer">
    <p>&copy; 2025 URL Shortener. All rights reserved.</p>
  </footer>
</body>
</html>
